package com.expenseguard.recurring.entity;

/**
 * Frequency options supported for recurring transaction templates.
 */
public enum RecurrenceFrequency {
    DAILY,
    WEEKLY,
    MONTHLY,
    YEARLY
}
